# context API are used to prevent props drilling.

# Context API is used to pass global variables anywhere in the code. It helps when there is a need for sharing state between a lot of nested components.